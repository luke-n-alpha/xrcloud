{
	"project_identity": {
		"name": "XRCLOUD",
		"symbol": "^",
		"nature": "Open-source metaverse service based on Hubs with XRCLOUD backend/frontend and a hubs-all-in-one stack.",
		"philosophy": "Minimal, traceable changes; preserve production stability; never expose secrets or private infrastructure details."
	},
	"architecture_rules": {
		"modification_levels": {
			"L1_independent": "docs/, work-logs/, scripts and notes that do not affect runtime.",
			"L2_conditional": "Root scripts, docker helpers, and README/docs that describe operations.",
			"L3_direct": "hubs-all-in-one/, xrcloud-backend/, xrcloud-frontend/, xrcloud-nginx/ (runtime-critical; document changes)."
		},
		"protection_rules": {
			"protected_directories": [
				"**/.env*",
				"**/certs",
				"**/*.pem",
				"**/nginx.conf"
			],
			"comment_required": "Explain production impact for changes to runtime scripts or server configs.",
			"max_changes": "Prefer minimal, targeted edits; avoid refactors without explicit approval."
		}
	},
	"ai_workflow": {
		"knowledge_principle": "AI knowledge = Developer knowledge (1:1 parity)",
		"mandatory_pre_checks": [
			"Read caret-rules.json first",
			"Identify work nature",
			"Apply minimal-change principles"
		]
	},
	"workflows": {
		"instruction": "Read workflows on demand. Do not load all at once.",
		"selection_guide": {
			"reference": ".agents/context/ai-work-index.yaml",
			"steps": [
				"Extract keywords from the user request",
				"Match the category in ai-work-index.yaml",
				"Read the matching root quick reference first",
				"Load the workflow document only if the task still needs details"
			]
		},
		"index": {}
	}
}
